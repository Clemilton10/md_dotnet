# While

```c#
int valor = 0;
// Verifica a condição antes de executar
while (valor < 5)
{
	valor++;
	Console.WriteLine(valor);
}
```

```c#
int valor = 0;
// Verifica a condição depois de executar
do
{
	valor++;
	Console.WriteLine(valor);
} while (valor < 5);
```
