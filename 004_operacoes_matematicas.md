## Operações matemáticas

```c#
int n = 22 / 5; // 4 -> arredonda
double h = 22 / 5; // 4.4

int x = 2 + 2 * 2; // 6
int y = 2 + (2 * 2); // 6
int z = (2 + 2) * 2; // 8

int x = 0;
x += 5; // x = x + 5;
x -= 5; // x = x - 5;
x *= 5; // x = x * 5;
x /= 5; // x = x / 5;
```
